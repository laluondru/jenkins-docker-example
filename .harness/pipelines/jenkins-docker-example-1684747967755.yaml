pipeline:
  identifier: Build_jenkins_docker_example_1684747976089
  name: Build jenkins-docker-example
  orgIdentifier: default
  projectIdentifier: Fruits
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github
        repoName: laluondru/jenkins-docker-example
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: dockerbuild
                  name: docker_build
                  spec:
                    image: plugins/docker
                    settings:
                      dry_run: "true"
                      repo: hello/world
                      tags: latest
                  timeout: ""
                  type: Plugin
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Cloud
        type: CI
